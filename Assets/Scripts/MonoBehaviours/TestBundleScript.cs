using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestBundleScript : MonoBehaviour
{
    public string test;

    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("A brand New Fighter! "+test);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
